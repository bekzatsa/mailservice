spring:
  application:
    name: mailservice

  mail:
    properties:
      mail:
        smtp:
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

    debug: true
    default-encoding: UTF-8

server:
  port: 0 #random port only set free ports


